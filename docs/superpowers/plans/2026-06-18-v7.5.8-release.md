# Version 7.5.8 Release Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Update every release-facing version surface and document the TP compatibility fix as version 7.5.8.

**Architecture:** `config.json` remains the runtime version source, while packaging and launcher filenames retain their existing explicit version strings. A Node regression test checks these surfaces and the bilingual release notes together.

**Tech Stack:** JSON, Python comments, Windows batch, Markdown, Node.js `node:test`

---

### Task 1: Add the failing version consistency test

**Files:**
- Modify: `tests/frontend-compatibility.test.js`

- [ ] Add a test that reads `config.json`, `app.py`, `start_autoplan.bat`, `README.md`, and `README.en.md`; assert version `7.5.8`, date June 18, 2026, and bilingual `v7.5.8` history headings.
- [ ] Run `pnpm test` and verify the new test fails because the repository still reports `7.5.7`.

### Task 2: Update release surfaces and documentation

**Files:**
- Modify: `config.json`
- Modify: `app.py`
- Modify: `start_autoplan.bat`
- Modify: `README.md`
- Modify: `README.en.md`

- [ ] Replace the current release version with `7.5.8` in configuration, the PyInstaller output name, launcher paths, and README header/footer metadata.
- [ ] Add Chinese and English `v7.5.8` release notes covering transpiled TP scripts, restored initialization, notification details, compatibility tests, and `AGENTS.md` guidance.
- [ ] Run `pnpm test` and verify all tests pass.

### Task 3: Rebuild, verify, and commit

**Files:**
- Verify generated frontend assets remain current.

- [ ] Run `pnpm build`, `pnpm test`, all compiled-script `node --check` commands from `AGENTS.md`, Python compilation, and `git diff --check`.
- [ ] Commit the tested release update with message `chore: release version 7.5.8`.
