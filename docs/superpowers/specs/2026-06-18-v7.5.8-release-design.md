# Version 7.5.8 Release Update

## Scope

Prepare the repository metadata and documentation for release `7.5.8`, dated
June 18, 2026. This is a release bookkeeping change for the completed TP
WebView compatibility fix; it does not add further runtime behavior.

## Version Surfaces

- Set `config.json` to `7.5.8`; this remains the application version displayed
  by the template and used for static asset cache keys.
- Update the PyInstaller command example in `app.py` to build
  `AutoPlan_V7.5.8`.
- Update every executable path and diagnostic message in `start_autoplan.bat`
  to reference `AutoPlan_V7.5.8.exe`.
- Update the current-version header and footer in `README.md` and
  `README.en.md`.

## Release Notes

Add a `v7.5.8` entry to both README version histories. The entry will document
that all first-party TP scripts are Babel-transpiled, button and translation
initialization no longer stops on unsupported syntax, TP notification details
are displayed, and automated compatibility checks plus `AGENTS.md` guidance
were added.

## Verification

Extend the Node test suite with a version-consistency test covering all release
surfaces and both README entries. Run the test before editing to confirm it
fails on `7.5.7`, then update the files, rebuild frontend assets, and run the
complete verification commands required by `AGENTS.md`.
